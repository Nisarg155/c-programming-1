#include<stdio.h>


int main() 
{
    int no_tree;
    typedef struct tree_info {
        char tree[20];
        int age ;
    } tree ;
    printf("Enter the number of tree outside the park :- ");
    scanf("%d",&no_tree);
    printf("Enter the number of parks ");
    scanf("%d")
    typedef struct park_info {
        tree t[n];
    }

    return 0;
}