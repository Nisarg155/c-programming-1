#include <stdio.h>
#include <math.h>
#define BLOCKS 10
#define MAX 10
int main()
{
    int count = 0, i = 0, turn = 0, val;
    int a[BLOCKS] = {0};
    int pl1[BLOCKS] = {0}, pl2[BLOCKS] = {0};

    while (1)
    {
        if (!(turn)) // player 1  even values
        {
            turn = 1;
            printf("\n turn of player 1");
            val =0;
        }
    }
}